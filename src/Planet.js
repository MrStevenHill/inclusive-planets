import React from "react";
import styled from "styled-components";

const PlanetContainer = styled.div``;

const Planet = ({ name }) => <PlanetContainer>{name}</PlanetContainer>;

export default Planet;
